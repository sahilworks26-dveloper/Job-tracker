PORT=5000
# MongoDB Atlas — replace USER, PASS, CLUSTER; add /job-tracker before ?
MONGODB_URI=mongodb+srv://USER:PASS@CLUSTER.mongodb.net/job-tracker?retryWrites=true&w=majority
JWT_SECRET=your_super_secret_jwt_key_change_in_production
# Optional: extra frontend origins (comma-separated, no /api path)
# CORS_ORIGIN=https://your-frontend.vercel.app
